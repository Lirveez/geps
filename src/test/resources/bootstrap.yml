spring:
  flyway:
    schemas: geps
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
spring.jpa.properties.hibernate.enable_lazy_load_no_trans: true